apiVersion: 1

# Root notification policy: route every alert to Telegram. Provisioned policies
# replace the default policy, so this must be a complete root route.
policies:
  - orgId: 1
    receiver: telegram
    group_by: ["grafana_folder", "alertname"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
